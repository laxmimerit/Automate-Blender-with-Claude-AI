# Blender Session — Production Prompts

1. Create cinematic studio lighting with warm key light, blue rim light, soft shadows, volumetric fog, and a slow 50mm cinematic dolly camera animation in Blender.

2. Rotate the selected 3D character in Blender for a complete 360-degree turntable animation across 72 frames, capture one high-quality render per frame from a fixed cinematic camera angle, use smooth lighting with soft shadows and transparent background disabled, then automatically compile all rendered frames into a smooth `.mp4` video at 24 FPS using FFmpeg, and save both the image sequence and final video inside the `~/Downloads/character_turntable/` directory.
